# tollbooth-dpyc is the shared SDK — all crypto, vault, auth, pricing, audit, payments.
# Every source/config change requires @lonniev review (CODEOWNERS + branch protection);
# the agentic Journeyman may open PRs but nothing lands without you. Only tests and docs
# are uncovered, so path-gated auto-merge can land those.
* @lonniev
/tests/
*.md
